//Fichier .draw qui permet de dessiner des formes sur l'IDE

var x, y, height, width, radius;

circle(x, y, radius);
rectangle(x, y, width, height);
line(x1, y1, x2, y2);
triangle(x1, y1, x2, y2, x3, y3);

